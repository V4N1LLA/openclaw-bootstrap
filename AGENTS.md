# OpenClaw 부트스트랩 하네스 에이전트 규칙

## 저장소 목적

- 이 저장소는 실제 서비스 코드 저장소가 아니라 OpenClaw, Telegram Bot, Codex CLI 자동화 준비를 돕는 부트스트랩 하네스 저장소다.
- 주요 산출물은 문서, 설정 템플릿, 검증 스크립트, 실행 절차 안내다.
- 실제 OpenClaw 서비스 코드, Telegram Bot 서비스 코드, 운영 애플리케이션 코드는 이 저장소에 생성하지 않는다.

## 작업 원칙

- 작업 전 루트 `AGENTS.md`와 사용자가 지정한 최소 파일만 확인한다.
- 사용자가 허용한 파일과 직접 관련된 파일만 읽고 수정한다.
- 전체 저장소를 무작정 탐색하지 않는다.
- 변경 범위는 요청된 하네스 문서, 설정, 스크립트 보완으로 제한한다.
- 같은 파일을 반복해서 읽지 않고, 필요한 섹션만 확인한다.
- 검증은 변경 범위에 맞는 최소 명령으로 수행한다.
- `git commit`은 사용자가 명시적으로 요청했을 때만 수행한다.
- `git push`, merge, deploy는 사용자 승인 없이 수행하지 않는다.

## 토큰 절약 원칙

- 처음에는 루트 규칙, 현재 작업 범위, 관련 문서만 읽는다.
- 검색 결과가 많으면 상위 5개 이하만 확인한다.
- 큰 파일은 전체를 출력하지 말고 필요한 부분만 읽는다.
- 로그는 전체 출력하지 말고 실패 원인에 필요한 마지막 부분이나 핵심 블록만 요약한다.
- `node_modules`, `dist`, `build`, `coverage`, `logs`, 바이너리, 이미지, 잠금 파일은 요청과 직접 관련 없으면 읽지 않는다.
- 필요한 파일이 5개를 넘으면 먼저 범위를 줄이거나 사용자에게 확인한다.
- 최종 보고는 변경, 검증, 남은 TODO 중심으로 짧게 작성한다.

## 보안 원칙

- token, secret, password, 인증 정보, 개인 식별 정보를 읽거나 출력하지 않는다.
- `.env`, `.env.*`, `*.env`, `openclaw.env`, token 관련 파일, `secrets/`는 읽지 않는다.
- Telegram Bot token은 절대 출력하지 않는다.
- 민감 정보가 필요해 보이면 작업을 멈추고 사용자가 직접 처리할 수 있는 절차를 안내한다.
- 민감 정보가 포함될 수 있는 로그나 설정 파일은 전체 출력하지 않는다.
- `.codex-context/`는 임시 컨텍스트 저장용으로만 사용하며 git에 포함하지 않는다.

## 금지사항

- 실제 서비스 코드를 생성하지 않는다.
- OpenClaw를 실행하지 않는다.
- Telegram token을 읽거나 출력하지 않는다.
- 외부 패키지를 설치하지 않는다.
- 사용자 승인 없이 `git push`, merge, deploy를 수행하지 않는다.
- 사용자 요청 없이 `git commit`을 수행하지 않는다.
- 허용 범위 밖 파일을 수정하지 않는다.
- 민감 정보가 있을 수 있는 파일이나 디렉터리를 탐색하지 않는다.

## 완료 보고 형식

작업 완료 후 다음 형식으로 짧게 보고한다.

1. 변경 요약
2. 변경 파일
3. 실행한 검증
4. 남은 TODO
5. 추천 커밋 메시지

## 추천 커밋 메시지 규칙

- 완료 보고에는 항상 추천 커밋 메시지를 포함한다.
- 커밋 메시지는 실제 변경 범위만 반영한다.
- 커밋 메시지는 한국어로 작성한다.
- 형식은 `:gitmoji: type: 한국어 요약`을 우선 사용한다.
- 하네스 문서 보완은 `:memo: docs:` 접두사를 우선 사용한다.
- 예: `:memo: docs: 하네스 루트 AGENTS 지침을 추가한다`
