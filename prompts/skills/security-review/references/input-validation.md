# 입력 검증 체크리스트

- 외부 입력은 길이, 형식, 범위를 검증한다.
- 파일 경로, shell 명령, SQL, URL에 들어가는 입력은 별도 검증한다.
- HTML 또는 markdown 입력은 렌더링 전 sanitize 정책을 확인한다.
- 업로드 파일은 확장자, MIME, 크기 제한을 확인한다.
- 서버 측 검증 없이 client 검증만 믿지 않는다.
- 에러 메시지는 내부 구조를 과도하게 드러내지 않는다.
